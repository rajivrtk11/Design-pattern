
//@controller
public class UpdateEmpolyee {
    // employee id, data
    // /updatePhone/12121
    // body => mobile
    /*

    PersonaleDetailService personlDetailService;
     @putMapping(/update/'mobile')
     updateMobile(@PathVariable('mobile') mobile, @RequestBody MoibleDto mobileDto) {
        personlDetailService.updateMobile(mobile, mobileDto)
        return ResponseEntity
     }


    // service
    PersonaleDetailService {
        updateMobile(String mobile, MoibleDto mobileDto) {
          MobileDataEntity mobile =  findByMobileNo(mobile);
          mobile.setMoibleNumber(mobileDto.getMobileNumber)
        }
    }

    MoibleDto {
     moibleNumber;
    }

    eg: [1,2,3,4,5,6]
    linear: o(n)
    binarySearch: o(log(n))

    lifo => last in first out
    eg. [1,2,3,4,5,6]
            4
           /  \
           2   5
          /  \   \
         1   3   6
     M-1
     Iterate throught the tree and get

     step -1 : get list

     List<Node> list = new Arraylist();

     list = [1, 2, 3, 4, 5, 6]
     k = 3;

     dfs(Node root) {
        if(root == null) return;

        if(root.left != null) {
            dfs(root.left);
        }

        list.add(root);
        if(root.right !=  null) {
            dfs(root.right;
        }
     }

       o(n)
       e {
           List<Project> project;
           emp.flatMap(e -> e.getProject().stream())
       }


    */

    /*
    How does the spring security works?

    */
}
